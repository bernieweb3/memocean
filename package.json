{
  "name": "memocean",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --filter apps/mcp-server dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "overrides": {}
  },
  "dependencies": {
    "@modelcontextprotocol/server-filesystem": "^2026.1.14"
  }
}
